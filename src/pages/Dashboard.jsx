import { PerformanceMetrics } from '../features/dashboard';
import { Box } from '@mui/material';

export default function Dashboard() {
  return (
    <Box>
      <PerformanceMetrics />
    </Box>
  );
}